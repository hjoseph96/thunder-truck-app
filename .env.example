# ============================================
# EXPO ENVIRONMENT VARIABLES
# ============================================
# 
# IMPORTANT: 
# - All public env vars MUST use EXPO_PUBLIC_ prefix (Expo SDK 54+)
# - These variables are inlined at build time and visible in compiled code
# - Do NOT store secrets here (API keys, tokens, etc.)
# - For native platforms (iOS/Android), some configs go in app.json instead
#
# To use:
# 1. Copy this file to .env (remove .example)
# 2. Fill in your values
# 3. Restart the dev server (npm run web / npm start)
# ============================================

# Builder.io API Key
# Get from: https://www.builder.io/c/docs/using-your-api-key
EXPO_PUBLIC_BUILDER_KEY=your_builder_io_key_here

# ============================================
# GOOGLE MAPS CONFIGURATION
# ============================================
# 
# Platform-specific behavior:
# - WEB: Uses this .env variable (EXPO_PUBLIC_GOOGLE_MAPS_API_KEY)
# - iOS: Uses app.json → ios.config.googleMapsApiKey
# - Android: Uses app.json → android.config.googleMaps.apiKey
#
# Get your API key:
# 1. Go to: https://console.cloud.google.com/google/maps-apis
# 2. Create a new project or select existing
# 3. Enable "Maps JavaScript API" (for web)
# 4. Enable "Maps SDK for iOS" (for iOS)
# 5. Enable "Maps SDK for Android" (for Android)
# 6. Create credentials (API Key)
# 7. Set restrictions (HTTP referrers for web, bundle IDs for mobile)
#
# IMPORTANT: Enable billing in Google Cloud Console
# You get $200 free credit per month
# ============================================

EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
